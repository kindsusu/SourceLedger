# Direct dependency versions used for the portable installation baseline.
# This is a constraints file, not a complete transitive lockfile.
# Optional packages are installed only when their extra is requested.
httpx==0.28.1
beautifulsoup4==4.15.0
XlsxWriter==3.2.9
openpyxl==3.1.5
pypdf==6.19.0
playwright==1.63.0
mcp==1.30.0
pytest==9.1.1
